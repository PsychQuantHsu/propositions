% Synthetic stub .tex for smoke test scenario duplicate_id.
% Mirrors prop texts in duplicate_id.jsonl so R1 prop-subset-check passes,
% allowing R8 unique-ids to fire on the duplicate UUID.

First prop with the duplicated UUID.

Second prop sharing the same UUID.
